name: Ruff

on:
  push:
    branches:
      - bot
  pull_request:
    branches:
      - bot

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: astral-sh/ruff-action@v4.0.0
      - run: ruff check --fix
      - run: ruff format